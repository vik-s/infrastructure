http:
  routers:
    router-weekly:
      rule: Host(`weekly.iyer.app`)
      service: service-weekly
      middlewares: localip@file
  services:
    service-weekly:
      loadBalancer:
        servers:
          - url: http://192.168.1.100:3000
